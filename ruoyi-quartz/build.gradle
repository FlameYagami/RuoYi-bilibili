group 'com.ruoyi.quartz'
description = 'ruoyi-quartz'

jar.enabled = true
bootJar.enabled = false

dependencies {
    implementation project(':ruoyi-common')

    implementation 'org.quartz-scheduler:quartz:2.3.2'
}
