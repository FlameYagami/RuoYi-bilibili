group 'com.ruoyi.http'
description 'Http模块'

jar.enabled = true
bootJar.enabled = false

dependencies {
    compile "com.squareup.okhttp3:okhttp:${okhttpVersion}"
    compile "com.squareup.retrofit2:converter-gson:${retrofitVersion}"
}